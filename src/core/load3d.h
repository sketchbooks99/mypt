#include "vec.h"

namespace mypt {

void loadObj(
    std::vector<vec3>& vertices, 
    std::vector<vec3>& normals, 
    std::vector<int3>& faces,
    std::vector<vec2>& texcoords) 
{

}

void loadPly(
    std::vector<vec3>& vertices,
    std::vector<vec3>& normals, 
    std::vector<int3>& faces, 
    std::vector<vec2>& texcoords
)
{
    
}

}